<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is required ONLY for the optional promotional video (if the owner
      configures a remote/YouTube source) and for opening the channel page.
      All note-taking functionality works fully offline.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RubeNotePad">

        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NotesActivity"
            android:exported="false" />

        <activity
            android:name=".EditorActivity"
            android:exported="false"
            android:parentActivityName=".NotesActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
